import Checklist from '@/components/Checklist'

export const metadata = {
  title: 'Item',
  description:
    'In this guide, we will look at how to work with paginated responses when querying the Protocol API',
}

# Item

## Critérios WCAG e eMAG
<Properties fullWidth>
  <Property name="WCAG 1.1.1 / eMAG Diretriz 3" type="Texto alternativo">
    Certifique-se de que o Item tenha um rótulo claro ou texto alternativo se for um elemento interativo.
  </Property>
</Properties>


## Desenvolvimento
<Row>
  <Col>
    - Implementar `aria-label` ou `aria-describedby` para garantir que os itens sejam acessíveis.
  </Col>
  <Col>
    ```bash {{ title: 'Acessibilidade no Desenvolvimento' }}
    curl -G https://api.protocol.chat/v1/conversations \
      -H "Authorization: Bearer {token}" \
      -d starting_after="s4WycXedwhQrEFuM" \
      -d limit=10
    ```
  </Col>
</Row>

## Design
<Row>
  <Col>
  - Definir rótulos claros para todos os itens interativos.
  </Col>
  <Col>
    <ImageNote
          imageSrc="https://placehold.co/600x200.png"
          imageAlt="Acessibilidade no Design"
        />
  </Col>
</Row>

### Checklist
<Checklist>
Implementar aria-label ou aria-describedby para garantir que os itens sejam acessíveis.
Definir rótulos claros para todos os itens interativos.
</Checklist>
